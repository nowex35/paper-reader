fastapi>=0.110
uvicorn>=0.27
httpx>=0.27
python-dotenv>=1.0
pydantic>=2.6
pywebview>=6.0
google-genai>=1.0   # 質問機能(Gemini)用。翻訳のみなら無くても動く
openai>=1.0         # 質問機能(OpenAI)用
anthropic>=0.30     # 質問機能(Claude)用
# openai-codex(質問機能/ChatGPTサブスク用)は依存に約200MBの codex バイナリを含むため、
# ここには載せず `pip install --no-deps openai-codex` で SDK のみ入れる（バイナリはシステムの codex CLI を検出して使う）
